{
  "name": "ratix",
  "version": "1.0.0",
  "description": "Ratix Firebase Game Application",
  "scripts": {
    "firebase": "firebase",
    "deploy": "firebase deploy",
    "serve": "firebase serve",
    "init": "firebase init",
    "login": "firebase login"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
